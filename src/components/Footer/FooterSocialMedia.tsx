import * as React from 'react';
// import { FormattedMessage } from 'react-intl';

import './styles.css';

const FooterSocialMedia: React.SFC<{}> = () => {
  return (
    <div className="footerSocialMedia">
      <p>Connect to us on Facebook etc.</p>
    </div>
  );
};

export default FooterSocialMedia;
